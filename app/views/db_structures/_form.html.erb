<%= form_for(@db_structure) do |f| %>
  <% if @db_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@db_structure.errors.count, "error") %> prohibited this db_structure from being saved:</h2>

      <ul>
      <% @db_structure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
