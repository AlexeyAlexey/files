<%= form_for(@file_folder) do |f| %>
  <% if @file_folder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_folder.errors.count, "error") %> prohibited this file_folder from being saved:</h2>

      <ul>
      <% @file_folder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
